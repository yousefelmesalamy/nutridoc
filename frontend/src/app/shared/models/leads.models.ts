export interface ContactPayload {
  name: string;
  email: string;
  phone: string;
  subject: string;
  message: string;
}

export interface PlanRequestPayload {
  name: string;
  email: string;
  phone: string;
  plan: string;
  message: string;
}
